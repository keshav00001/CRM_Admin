import React from "react";
import {
  Avatar,
  Box,
  Card,
  CardContent,
  CardHeader,
  Grid,
  Typography,
} from "@mui/material";

export default function TrafficByDevice(props) {
  return (
    <Card sx={{ height: "100%" }} {...props}>
      <CardHeader title="Traffic by Device" />
      <CardContent></CardContent>
    </Card>
  );
}
